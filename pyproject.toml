[project]
name = "a_maze_ing"
version = "0.1.0"
description = "Awesome project"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.black]
line-length = 79