# from utils.draw import cursor, color


# class PathDrawer:
#     def __init__(self, path: list[tuple[int, int]]):
#         self.path = path

#     def path_draw(self, offset_line: int, offset_col: int) -> None:
#         dot = color("•", 255, 0, 0)
#         for pos in self.path:

#             # actual_pos = (pos[0] + offset_line, pos[1] + offset_col)
#             print(cursor(actual_pos, dot), end="", flush=True)
